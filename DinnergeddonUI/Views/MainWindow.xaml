<Window x:Class="DinnergeddonUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DinnergeddonUI.Views"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />

    </Window.Resources>
    <DockPanel>
        <Border Padding="15
                 " Background="Gray" DockPanel.Dock="Left"    Visibility="{Binding Path=IsAuthenticated, Converter={StaticResource BoolToVisConverter} }">
            <StackPanel   >
                <TextBlock Padding="8" FontSize="18" Text="{Binding Username}"></TextBlock>
                <Button Padding="8" Content="Lobbies" Command="{Binding GoToLobbies}"></Button>
                <Button Padding="8" Content="Profile" Command="{Binding GoToProfile}"></Button>
                <Button Padding="8" Content="Highscores" Command="{Binding GoToHighscores}"></Button>
                <Button Padding="8" Content="Log out" Command="{Binding LogoutCommand}"></Button>
            </StackPanel>
        </Border>
        <ContentControl Content="{Binding CurrentPageViewModel}" />
    </DockPanel>
</Window>
