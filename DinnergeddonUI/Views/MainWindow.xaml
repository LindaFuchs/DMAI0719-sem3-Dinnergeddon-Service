<Window x:Class="DinnergeddonUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DinnergeddonUI.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1280">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />

    </Window.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock="Left" Background="Blue"   Visibility="{Binding Path=IsAuthenticated, Converter={StaticResource BoolToVisConverter} }">
            <TextBlock Text="{Binding Username}"></TextBlock>
            <Button Content="Lobbies" Command="{Binding GoToLobbies}"></Button>
            <Button Content="Profile" Command="{Binding GoToProfile}"></Button>
            <Button Content="dummy"></Button>
            <Button Content="Log out" Command="{Binding LogoutCommand}"></Button>
        </StackPanel>
        <ContentControl Content="{Binding CurrentPageViewModel}" />
    </DockPanel>
</Window>
